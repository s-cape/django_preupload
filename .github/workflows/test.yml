name: Test

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.8", "3.10", "3.12"]
        django-version: ["3.2", "4.2", "5.0"]
        exclude:
          - python-version: "3.8"
            django-version: "5.0"
          - python-version: "3.12"
            django-version: "3.2"

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -e ".[dev]" "Django==${{ matrix.django-version }}"

      - name: Run tests
        run: |
          python -m django test preupload.tests --settings=preupload.tests.settings -v0 --exclude-tag=integration

      - name: Black
        run: black --check .

  integration:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.10"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -e ".[dev]" "Django>=3.2"
          playwright install chromium

      - name: Run integration test
        run: |
          python -m django test preupload.tests.test_integration --settings=preupload.tests.settings -v0 --tag=integration
